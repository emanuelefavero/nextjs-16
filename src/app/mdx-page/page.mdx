import { Component } from './components/component'
import MdxComponent from './components/mdx-component.mdx'

# MDX Page

This is an example of an **MDX page** in a _Next.js_ application

We can import and use both regular React components and MDX components here:

<Component />
<MdxComponent />
