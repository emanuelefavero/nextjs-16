@baseUrl = http://localhost:3000

# NOTE: Replace with an actual product ID from your database
@productId = ReplaceWithActualProductID

### Get all products
GET {{baseUrl}}/api/products

### Create a new product
POST {{baseUrl}}/api/products
Content-Type: application/json

{
  "name": "New Product",
  "quantity": 5,
  "category": "Electronics"
}

### Delete a product by ID
DELETE {{baseUrl}}/api/products?id={{productId}}
